{% extends 'base.html.twig' %}
{% form_theme form 'bootstrap_5_layout.html.twig' %}

{% block body %}

    {{ form_start(form) }}

        {{ form_label(form) }}
        {{ form_widget(form) }}
        {{ form_errors(form) }}
    <button class="btn btn-primary"
            type="submit"
    >OK</button>

    {{ form_end(form) }}


{% endblock %}